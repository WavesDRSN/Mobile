<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <!-- ЭТО ВРЕМЕННОЕ МЕНЮ -->
    <group>
        <item
            android:id="@+id/nav_item_settings"
            android:icon="@android:drawable/ic_menu_preferences"
            android:title="Настройки" />
        <item
            android:id="@+id/nav_item_calls"
            android:icon="@drawable/ic_call"
            android:title="Звонки" />
        <item
            android:id="@+id/nav_item_new_chat"
            android:icon="@android:drawable/ic_menu_add"
            android:title="Новый чат" />
    </group>
    <item android:title="Прочее">
        <menu>
            <item
                android:id="@+id/nav_item_about"
                android:icon="@android:drawable/ic_menu_info_details"
                android:title="О нас" />
            <item
                android:id="@+id/nav_item_logout"
                android:icon="@android:drawable/ic_menu_revert"
                android:title="fdsfds" />

        </menu>
    </item>
</menu>